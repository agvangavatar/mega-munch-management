Tkinter is a simple GUI package made for Python, which is why I have rejected the rest of the GUI packages since I can simply use Python's code inside Visual Studio Code. Tkinter fits the MVP philosophy since it is simple to apply, which therefore saves time, and due to its simplicity it is easy to maintain. The limitation of Tkinter according to reviews is that  it has missing functionalities such as drag and drop.